fastapi==0.116.1
uvicorn[standard]==0.35.0
sqlalchemy==2.0.42
alembic==1.16.4
python-dotenv==1.1.1
pydantic==2.11.7

# JWT 발급/검증(app/core/auth.py, app/core/security.py, app/core/token.py,
# app/core/dependency/auth.py, app/domains/auth/router.py,
# app/domains/auth/dependencies.py)에서 직접 import한다 — 기존 venv에
# 우연히 설치돼 있었을 뿐 requirements.txt 선언이 누락돼 있었다
# (2026-08-01 Packaging Gate 0 감사로 발견, 신규 설치 아님 — 이미
# 검증된 버전을 그대로 선언).
python-jose==3.5.0

# 설정 로딩(app/core/config.py)에서 직접 import한다 — 위와 동일한
# 사유로 누락됐던 선언.
pydantic-settings==2.14.2

# app/domains/user/schema.py의 Pydantic EmailStr 필드가 런타임에
# 요구하는 선택적 의존성이다 — 현재 venv에는 우연히 설치돼 있었을 뿐
# requirements.txt 선언이 누락돼 있었다(2026-08-01 Packaging Gate 1
# 깨끗한 환경 검증에서 발견 — 650개 테스트 중 1 failure + 8 error가
# 전부 이 누락 하나로 귀결됨을 확인). 이미 검증된 버전을 그대로 선언.
email-validator==2.3.0

# FastAPI의 UploadFile/File 폼 파싱에 필요한 런타임 의존성이다.
# purchase_task CSV preview 라우터가 직접 요구하지만 기존 개발 venv에만
# 우연히 설치되어 clean 패키지에서 app.main import가 실패했다(E1002).
python-multipart==0.0.32

# 비밀번호 해시(app/core/security.py) + 네이버 client_secret_sign
# (app/domains/store_connection/adapters/naver_signing.py)에서 직접
# import한다 — passlib을 거치지 않는다(2026-07-30: passlib의 bcrypt
# 버전 감지 코드가 5.0.0과 충돌해 로그인이 항상 실패하던 문제를
# passlib 우회로 해결했다. 그 이후 코드는 이 정확한 버전으로만
# 검증됐다).
bcrypt==5.0.0

# Desktop Shell (app/desktop/**) — Windows 독립 창(WebView2 기반)
pywebview==6.2.1

# 이미지 배경 합성(app/domains/media_asset/composition.py) — 실제 제품
# 누끼 이미지 위에 배경을 합성/리사이즈한다. 순수 로컬 처리, 외부
# API·네트워크 호출 없음. 2026-08-20 CTO 승인(이미지 Workflow 기능
# 추가) — image_validation.py의 기존 "Pillow 미사용" 원칙은 유지하되
# (기존 signature 파싱은 그대로 표준 라이브러리만 사용), 신규 합성
# 기능에서만 명시적으로 사용한다.
# 2026-08-20 Live Gate L-2 — 최초 11.0.0 고정이 rembg==2.0.81(아래,
# pillow>=12.1.0,<13.0.0 요구)과 실제로 충돌해 clean venv 설치가
# 실패함을 이번 패키징 사전검증에서 실측 확인했다. 기존 dev venv는
# 이 고정값과 무관하게 이미 12.3.0이 설치돼 있었고(2091개 테스트가
# 전부 이 버전으로 통과한 상태) requirements.txt만 낡아 있었다 —
# 실제로 검증된 그 버전으로 선언을 맞춘다.
Pillow==12.3.0

# 로컬 ML 기반 배경 제거(누끼, app/domains/media_asset/
# image_processing_providers.py::RembgBackgroundRemovalProvider) —
# 유료 API 아님, 완전 로컬 추론. onnxruntime을 함께 설치한다(rembg의
# 필수 의존성). 최초 실행 시 사전학습 모델(u2netp, 약 4.7MB — 가장
# 작은 모델을 명시적으로 선택)을 한 번 내려받는다(네트워크 호출,
# 첫 실제 사용 시에만 발생 — 이번 라운드의 격리 테스트는 FakeProvider
# 만 사용해 이 다운로드 자체를 실행하지 않았다, 최종 보고에 명시).
# 패키징(공식 빌드 도구) 크기 영향: onnxruntime이 수십MB 추가됨 —
# 공식 재빌드 전 별도 확인 필요.
# 버전: rembg==2.0.61은 Python 3.13을 지원하지 않아(Requires-Python
# <3.13) 설치가 거부됨 — 이 프로젝트의 실제 인터프리터(3.13.14)에서
# 설치 가능한 최신 버전으로 실측 확정했다.
rembg==2.0.81
onnxruntime==1.29.0

# 쿠팡 Live 상품등록 HMAC 서명 호출(app/domains/marketplace_listing/
# coupang_live_provider.py)에서 이미 직접 import하고 있었다 — 기존
# 개발 venv에 우연히 설치돼 있었을 뿐 선언이 누락돼 있었다(2026-08-27
# V7 완성 감사에서 발견, 신규 설치 아님 — 이미 검증된 버전을 그대로
# 선언).
requests==2.34.2

# 상품 이미지 공개 호스팅(Cloudflare R2, S3 호환 API) —
# app/domains/media_asset/public_hosting.py. 쿠팡이 vendorPath로
# 이미지를 가져가려면 공개 HTTPS URL이 필요한데 HOMEZ는 이미지를
# 로컬 디스크에만 저장하므로, 승인된 이미지를 업로드해 공개 URL을
# 만드는 용도로만 사용한다.
boto3==1.40.15

# 2026-09-06 "가격·재고 자동 모니터링"(app/core/scheduler_service.py,
# app/domains/price_monitoring/**) — 주기 실행 인프라. 데스크톱 앱
# 프로세스 내부에서 BackgroundScheduler로 동작하며, 외부 메시지
# 브로커나 별도 워커 프로세스를 두지 않는다(Modular Monolith 원칙
# 유지). 사용자 승인 하 신규 의존성 추가.
apscheduler==3.10.4

# 2026-09-06 "가격·재고 자동 모니터링" 확장 — 자바스크립트로 상품을
# 그리는 사이트(에누리/11번가/무신사 등, 서버 차단이 아니라 정적
# HTML에 상품이 없을 뿐임을 실측 확인) 대응
# (app/domains/price_monitoring/adapter.py::
# HeadlessBrowserPriceStockAdapter). 표준 headless Chromium 실행만
# 하며 지문 위장·프록시 로테이션 등 봇 탐지 회피 기법은 쓰지 않는다.
# 브라우저 바이너리는 별도로 `playwright install chromium`으로
# 설치해야 한다(패키지 설치만으로는 부족 — 설치 스크립트/문서에 반영
# 필요, 이번 세션은 개발 venv에 직접 설치 완료).
playwright==1.55.0
