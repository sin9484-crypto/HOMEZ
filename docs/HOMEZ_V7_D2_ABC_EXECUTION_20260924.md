# HOMEZ V7 — D2 실행: A/B/C 실제 적용 (2026-09-24, 19차)

이 라운드에서 **A(원본 Migration 3건 적용)·B(CH1147184 `NEEDS_REVIEW` 기록)·
C(CH1147184 상품 조회 1회)를 실제로 실행했다** — 명시적 승인 범위 안에서만
실행했고, 승인 밖(검토 해제, 판매신청 POST, 쿠팡 등록, 실주문·발주·결제·
반품·환불, 임의의 상품 최종 선택)은 전혀 수행하지 않았다.

## 기준선 확인

`git fetch` 후 재확인: 로컬 HEAD = 원격 `origin/main` HEAD = `f09c398`
(18차 커밋). 다른 작업자의 워크트리 3개 보존, 전혀 건드리지 않음. 코드
변경이 없어(이번 라운드는 DB·데이터 작업뿐) 전체 회귀·집중 회귀를 다시
실행하지 않았다(18차까지의 189건 결과 그대로 유효).

## ① A/B/C 각각의 실행·성공·실패·보류 여부

| 항목 | 결과 |
|---|---|
| A(Migration 3건 적용) | **실행·성공** |
| B(CH1147184 NEEDS_REVIEW 기록) | **실행·성공** |
| C(CH1147184 상품 조회 1회) | **실행·성공**(사용자에게 부수 효과 하나를 추가 확인받은 뒤 진행, 아래 §추가 확인 참고) |
| 검토 해제·판매신청 POST·쿠팡 등록·실주문·발주·결제·반품·환불 | **미실행**(승인 범위 밖, 이번에 요청받지 않음) |

### 실행 중 추가로 확인받은 사항

C 실행 직전 코드를 재확인하다가, 승인안에 명시된 "연결 상태·`verified_at`
갱신" 외에 `lookup_product()`가 자동으로 `ProductAttributeMatchService.
run_comparison()`도 실행해 `product_attribute_comparison_runs`에 비교
기록 1건을 남긴다는 사실을 발견했다. 승인안 문구와 차이가 있어 실행을
멈추고 사용자에게 이 부수 효과를 포함해 진행해도 되는지 확인했다 —
"포함해서 진행" 승인을 받은 뒤에만 C를 실행했다(§③에서 실제 기록
확인).

## ② 백업과 DB 검증 결과

### A. Migration 적용

| 항목 | 값 |
|---|---|
| 백업 위치 | `C:\Users\Daum pc\Homez-Backups\v7_migration_18th_20260924\homez_pre_migration_18th.db` |
| 백업 방식 | SQLite 공식 백업 API(`sqlite3.Connection.backup()`) — 원본은 읽기 전용 연결로만 접근 |
| 백업 검증 | `integrity_check` ok, 157개 테이블 전체 행 수가 원본과 완전히 일치(백업 직후 대조) |
| 적용 전 원본 해시 | `f3aafca1bf1e68af154f1a17c362607ed05c0683744c14d6623436ca76762008`(백업 파일 소스 해시와 동일, 14~18차에서 반복 확인된 값과도 동일) |
| 미적용 목록 재확인 | `plan_pending()`을 실 DB에 직접 재실행 — 승인안의 3건과 파일명·순서·체크섬 완전히 일치 확인 후에만 적용 진행 |
| 적용 순서 | `20260918_00_create_order_collection_test_budget_usage_schema.sql` → `20260921_00_create_supplier_option_link_schema.sql` → `20260924_00_add_marketplace_submissions_live_claim_index.sql`(전부 성공) |
| 적용 후 `integrity_check` | ok |
| 적용 후 `foreign_key_check` | 위반 0건 |
| 기존 데이터 보존 | 157개 테이블 중 `schema_migrations`(73→76, 이번에 적용된 3건이 정확히 기록됨) 외 **156개 테이블 전부 백업과 행 수 완전히 일치** — 기존 데이터 무변경 확인 |
| 새 테이블 | `order_collection_test_budget_usages`(0행, 신규 생성) / `supplier_option_links`(0행, 신규 생성) |
| 새 인덱스 | `uq_marketplace_submissions_live_claim_per_listing` — `sqlite_master`에 정확한 정의로 존재 확인 |
| ORM 호환 | SQLAlchemy `MarketplaceSubmission`/`SupplierOptionLink` 모델로 실제 쿼리 성공, 인덱스가 `inspect().get_indexes()`에도 정확히 반영됨 |
| 재적용 방지 | `apply_pending()` 재호출 시 빈 목록 반환(안전한 no-op) — 확인 완료 |
| 적용 후 원본 해시 | `dbc6f96614d68f83058cc35695ef5b660ac4074b77318d4d42343c012fcd3cae`(스키마 변경을 정확히 반영, 예상된 변화) |
| 적용 후 파일 크기 | 3,764,224 bytes(적용 전 3,702,784 bytes, +61,440 bytes — 새 테이블·인덱스만큼 증가, 정상) |
| 설치본 DB | 건드리지 않음(대상에서 애초에 제외) |
| 실패 여부 | 실패 없음 — 복구 절차 발동하지 않음 |
| 자동 실행 상태 | 스케줄러·서버 전부 비활성 유지(적용 전후 `python.exe` 프로세스 0개 확인) |

## ③ 실제 외부 호출 횟수와 로컬 변경 범위

### B. CH1147184 `NEEDS_REVIEW` 기록

| 항목 | 값 |
|---|---|
| 대상 | `company_id=1`, `connection_id=4`(ONCHANNEL, `sin9**`, 재확인) |
| 방법 | `PurchaseSalesApplicationService.record_unconfirmed_prior_evidence()`(검증된 서비스 경로, 실 DB에 대해 실행) |
| 결과 | 신규 행 id=1, `status=NEEDS_REVIEW` |
| 사건 발생 추정 시각 | 2026-09-14(과거 접수 근거 시각, 기존 감사 문서 근거) |
| 기록(확인) 시각 | 2026-09-24T07:38:54(자동, 서버 시각 — 사건 시각과 분리 저장) |
| 기록자 | `user#1`(회사 유일 사용자, 실제 계정) |
| 중복 방지 | 기록 전 `get_attempt()`로 기존 행 없음(`None`)을 먼저 확인한 뒤에만 진행 |
| 외부 호출 | **0회**(이 메서드는 애초에 Adapter·Credential Store를 전혀 참조하지 않는 구조 — 코드 확인) |
| 감사 기록 | `audit_logs`에 `SALES_APPLICATION_PRIOR_EVIDENCE_RECORDED`(id=29) 1건 생성 확인 |
| 의미 | **성공 확인이 아니라 확인 전 실행 차단 기록**이다 — `SATISFIES_ORDER_GATE`에 포함되지 않으며, 판매신청 POST·발주 자동 실행을 계속 막는다 |
| 검토 해제 | **수행하지 않음**(승인 범위 밖, 여전히 `NEEDS_REVIEW`로 남아 있음 — §C 이후에도 재확인) |
| 확인 방법 | 로컬 조회로만 확인(위 표 전부 읽기 전용 조회 결과) — 실제 발주 함수는 호출하지 않았다 |

**투명하게 밝히는 사소한 정정**: 기록 직후 콘솔 인코딩 문제로 `evidence_
summary` 입력값 일부가 로마자 표기로 저장된 것을 발견해, 해당 행의
`failure_detail`(운영 데이터, 감사로그 아님)만 정확한 한글 표기로
수정했다(사실·시각·상태 값은 전혀 변경하지 않음). 같은 문제로 로마자
표기가 남은 `audit_logs`의 `description` 필드는 **감사 기록 변조 방지
장치가 정확히 차단**해 수정하지 않았다 — 그 필드는 구조화된 사실
(연결 id·상품코드·사건 시각·근거 출처)은 전부 정확하고, `evidence_
summary` 자유 텍스트 부분만 로마자로 남아 있다(내용은 동일, 표기만
차이).

### C. CH1147184 상품 조회 1회

| 항목 | 값 |
|---|---|
| 방법 | `PurchaseChannelConnectionService.lookup_product(4, 1, "CH1147184", triggered_by=1)` — 실제 `WindowsCredentialStore` 기본값(주입 없음) |
| 호출 전 경로 재확인 | `channel_connection_service.py::lookup_product()` → `adapter.lookup_product()` → `onchannel_client.py::get_product()` → `_get()` → `self._http_get()` **1회만** 호출되는 것을 코드로 직접 추적해 확인(재시도·페이지네이션 코드 없음) |
| 실제 외부 호출 횟수 | **1회**(GET `/openapi/seller/product/CH1147184`) — 자동 재시도 없음, 추가 인증 호출 없음, 다른 상품 조회 없음 |
| 결과 | 성공(`support=SUPPORTED`) |
| 상품명 | 레이펄스 워터리스 샴푸/ 바디워시 |
| 옵션 4종(실제 응답, 2026-09-24 확인) | `3945578` 샴푸 200ml 본품 7,150원 / `3945579` 샴푸 500ml 리필 11,000원 / `3945580` 바디워시 200ml 본품 5,050원 / `3945581` 바디워시 500ml 리필 7,850원 — **2026-09-21 문서의 "과거 관측" 값과 코드·가격 완전히 동일** → 최소 이 기간 동안 가격 변동 없음을 실제로 확인 |
| 재고(`amount`) | 4개 옵션 전부 **미제공**(`in_stock=None`) — 이전과 동일하게 여전히 미확인 |
| 배송 정보(실제 응답) | `send_type=개별 배송비`, `quantity_threshold=1`, 기본 배송비 3,000원, 제주 6,000원, 도서산간 8,000원 — 필드 자체는 명확히 확인됐으나 "합산인지 대체인지"는 이 필드 구조만으로는 여전히 확정할 수 없음(API가 별도 필드로 분리 제공 — 실제 청구 시 합산 방식은 발주 시점에 별도 확인 필요) |
| 제공되지 않은 항목 | 상품 상태(`status`/판매중 여부는 이 상세조회 응답 구조에 없음 — 별도 목록조회 API 대상), 승인 상태(원천적으로 API 없음, 기존 확정 재인용) |
| 로컬 DB 변경(연결) | `connection.verified_at`: `2026-09-08 12:17:19` → `2026-09-24 07:43:59`(갱신), `status`: `CONNECTED`(불변), `consecutive_failure_count`: `0`(불변), `last_checked_at`: `2026-09-24 07:43:59`(갱신) — 승인안 명시 범위와 정확히 일치 |
| 로컬 DB 변경(부수, 사전 확인·승인받음) | `product_attribute_comparison_runs`에 1행(id=1) 생성 — 비교 기준값이 매입처 조회 하나뿐이라 다른 항목은 빈 값으로 전달, 판매채널·HOMEZ 현재값과의 실제 비교는 이뤄지지 않음(구조상 `UNCONFIRMED` 계열 결과로 남을 뿐, 등록·발주 판정에 아무 영향 없음) |
| `purchase_sales_application_attempts` 영향 | **없음**(이 호출 자체는 이 테이블에 아무것도 쓰지 않는다는 것을 재확인) — 조회 성공을 판매신청 승인으로 해석하지 않았고, `NEEDS_REVIEW` 상태도 그대로임을 호출 후 재조회로 확인 |
| 자격증명 원문 | 로그·출력 어디에도 남기지 않음(출력 파일에 `auth_key`/`password`/`bearer`/`token` 등 자격증명류 문자열 없음을 직접 검색해 확인) |

## ④ 확인된 상품·옵션 정보 및 미확인 사항

준비표(18차)를 이번 실제 조회 결과로 갱신한다:

| 항목 | 이전(18차) | 이번 확인 후 |
|---|---|---|
| 옵션 코드·가격 | 과거 관측(시점 미상) | **확인됨**(2026-09-24 실제 응답, 18차 과거 관측값과 동일 — 가격 불변 확인) |
| 재고(옵션별) | 미확인 | **여전히 미확인**(API가 제공하지 않음, 추측 없이 그대로 유지) |
| 배송비 구조 | 참고용(확정 아님) | **필드 자체는 확인됨**(개별 배송비, 기본 3,000/제주 6,000/도서산간 8,000) — 합산·대체 여부만 여전히 미확정 |
| SKU | 미입력 | **여전히 미입력**(Wizard 재개 시 사람이 입력) |
| 구성수량 | 미확인 | **여전히 미확인** |
| 공급가(실제 청구액과 옵션 price 일치 여부) | 미확인 | **여전히 미확인**(옵션 price가 실제 매입 청구가와 같은지는 발주 시점에만 확인 가능) |
| 판매가 | 미정 | **여전히 미정**(쿠팡 수수료율 미조사, 목표 마진 미산정) |
| 자료 이용권리 | 미확인/미설정 | **여전히 미확인**(Wizard `liveImageRightsConfirmed`류 필드, 이번 조회와 무관) |
| 판매 제한(판매자 자격) | 미확인 | **여전히 미확인** |
| 판매신청 상태 | 내부 0건 | **NEEDS_REVIEW로 기록됨**(§B) — 여전히 차단, 해제 안 함 |
| 발주 승인 | `PurchaseOrderApproval#1` 만료 | 변경 없음, 여전히 만료·재사용 금지 |
| 연결 인증 최신성 | 16일 경과 | **갱신됨**(§C, `verified_at=2026-09-24 07:43:59`) |

**미확인 값은 여전히 추측하지 않는다** — 이번 조회로 새로 확인된 항목만
표에 반영했고, 나머지는 그대로 차단 사유로 남겼다.

## ⑤ 다음 상품등록 단계에 필요한 최소 결정·승인

1. **판매신청 상태 확인**(별도 승인 필요, 문의 발송 포함) — `NEEDS_
   REVIEW`를 해제하려면 계정·상품이 일치하는 공식 확인 근거(온채널
   판매자센터 화면 직접 확인 또는 공급처 공식 문의 회신)가 있어야
   한다. 이번 라운드는 그 확인 자체를 승인받지 않았다.
2. **검토 해제 실행**(§1의 근거가 확보된 뒤에만, 별도 승인) — 17차의
   `resolve_needs_review_as_confirmed_submitted()`로 실행.
3. **Wizard#1 재개**(별도 승인) — SKU·구성수량·공급가 확정, 자료
   이용권리 확인, 판매 제한(판매자 자격) 조사, 새 `PurchaseOrderApproval`
   발급(기존 것은 만료).
4. **판매가 산정**(사용자 결정) — 쿠팡 수수료율 조사 후 목표 마진
   기준으로 확정.
5. 위 1~4가 모두 채워진 뒤에만 실제 쿠팡 등록(13차 중복등록 방어
   적용 상태)을 별도 승인받아 진행한다.

## 검증·Git

이번 라운드는 코드를 변경하지 않았다 — DB·데이터 작업만 수행했으므로
회귀 테스트를 재실행하지 않았다(18차까지의 189건 결과 재사용). 임시
서버·감시 작업을 띄우지 않았다(`python.exe` 0개로 시작·종료 재확인).

이번 라운드 소유 파일만 커밋한다: 이 문서, `docs/HOMEZ_PROJECT_STATE.md`
갱신. **DB·백업·자격증명·개인정보는 Git에 포함하지 않는다**(`homez.db`는
`.gitignore` 대상, 백업 파일은 저장소 밖 `Homez-Backups`에만 존재). 다른
작업자의 워크트리는 건드리지 않았다.
