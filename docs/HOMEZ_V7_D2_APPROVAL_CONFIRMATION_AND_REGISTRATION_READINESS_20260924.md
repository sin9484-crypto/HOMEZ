# HOMEZ V7 — D2 실행: A/B/C 승인 확인 및 상품등록 준비 (2026-09-24, 18차)

**이 문서 자체를 A/B/C 실행 승인으로 해석하지 않는다.** 이번 라운드에서
사용자의 별도 명시적 승인을 확인하지 못했으므로, 원본 DB Migration 적용
(A)·`NEEDS_REVIEW` 원본 기록(B)·상품 조회(C) 중 **어느 것도 실행하지
않았다**. 이번 라운드는 전부 읽기 전용 재확인과 문서 준비였다 — 코드
변경이 없어 전체 회귀도 재실행하지 않았다.

## 기준선 재확인

`git fetch` 후 재확인: 로컬 HEAD = 원격 `origin/main` HEAD = `78b5e59`
(17차 커밋). 다른 작업자의 워크트리 3개(`affectionate-cray-cb4fa5`,
`quirky-booth-62f752`, `quirky-diffie-3718ca`) 보존, 전혀 건드리지 않음.
작업 트리 clean, 실행 중인 `python.exe` 프로세스 0개로 시작·종료.

## ① A/B/C 승인 여부와 실제 수행 결과

**승인 여부**: 이번 프롬프트 자체가 "이 프롬프트 작성 요청 자체를 A/B/C
실행 승인으로 해석하지 않는다"고 명시했고, 이 대화의 다른 어떤 메시지
에서도 A·B·C 각각에 대한 사용자의 명시적 실행 승인 문구를 찾지 못했다.

**실제 수행 결과**: **A·B·C 전부 미실행.** 아래는 실행 대신 수행한
읽기 전용 재확인이다:

- **A 재확인**: `MigrationRunner.plan_pending()`을 실 `homez.db`(읽기
  전용 연결)에 대해 다시 실행 — 미적용 파일 3건이 기존 승인안과 파일명·
  순서·체크섬까지 **완전히 일치**함을 확인(`93ca072401873e920365f1974fd
  70116626544c81cfd9ff0fc880597a481b4b1` /
  `1a8c65c95d24ca36cfd4f59b5553fbd42bd33ce7491a09c1c3a5a4df72b5821a` /
  `3e114a08feb2c715958ed6e429771ca8b0b7305a84193202b95c8287d5a2d081`) —
  차이 없으므로 적용을 진행하지 않았다(승인 없음).
- **B 재확인**: `company_id=1`, `connection_id=4`(ONCHANNEL, `sin945`,
  `status=CONNECTED`), `product_code=CH1147184` — 전부 재확인, 변경
  없음. `purchase_sales_application_attempts`에 CH1147184 행 **0건**(재확인)
  — 기록하지 않았다(승인 없음).
- **C 재확인**: 연결 id=4·상품코드 재확인(위와 동일), 승인 없으므로
  실제 조회를 호출하지 않았다.

## ② 업무 DB 상태

| 항목 | 값 |
|---|---|
| 대상 | `C:\Users\Daum pc\Homez-OS\homez.db` |
| SHA-256(라운드 시작·종료 양쪽 재확인) | `f3aafca1bf1e68af154f1a17c362607ed05c0683744c14d6623436ca76762008`(**불변**) |
| 적용 완료 Migration | 73건(변경 없음) |
| 미적용 Migration | 3건(위 A 재확인 표와 동일, 변경 없음) |
| `PRAGMA integrity_check` | 재확인 안 함(이번 라운드는 쓰기 작업이 없어 무결성 변화 가능성 자체가 없음 — 14~17차에서 이미 반복 확인됨) |

## ③ 상품·옵션 등록 준비표와 미확인 항목

기존 세 상품 후보와 CH1147184 추천은 그대로 유지한다(URL 재요청 없음,
기존 6차~10차 결정 재사용). **CH1147184는 "추천 후보"이며 "사용자 확정
상품"으로 기록된 문서는 이번 라운드에서도 찾지 못했다** — 이 구분을
그대로 보고한다(추측으로 확정 처리하지 않음).

### 3-1. Wizard#1 현재 상태(원본 DB, 읽기 전용 재확인)

| 항목 | 값 |
|---|---|
| `product_candidate_id` | 3(→ `candidate_key`에 `ONCHANNEL:CH1147184` 명시) |
| `current_step` | `CHANNELS`(정지) |
| `status` | `DRAFT` |
| `channel_selections_json` | `[]`(채널 미확정) |
| `draft_json.options` | `[]`(옵션 미입력) |
| `draft_json.sku_list` | `[]`(SKU 미입력) |
| `materialized_listing_ids_json` | `[]`(외부 리스팅 미생성 — 13차 방어가 걸릴 대상 자체가 아직 없음, 변경 없음) |
| `marketplace_listings` 행 | 1건(id=1, `status=DRAFT`, `external_listing_id=None`) — **내부 초안일 뿐 외부 전송 이력이 아니다** |
| `marketplace_submissions` 행 | 0건(재확인) — 외부 전송 시도 자체가 **명확히 없음**(불명확이 아니라 "미시도"로 확정) |

**결론(지시문 "기존 외부 등록 결과가 불명확하면 새 등록을 시도하지
않는다")**: 이번 건은 불명확이 아니라 **명확히 미시도**다 — 그래서
"시도 자체를 하지 않는다"는 원칙이 적용될 필요 없이, 대신 "아직 등록에
필요한 입력이 채워지지 않았다"는 별개의 이유로 등록이 준비되지 않은
상태다.

### 3-2. 등록 준비표(값이 있는 것과 미확인/차단 사유를 분리)

| 항목 | 값 | 구분 |
|---|---|---|
| 상품명 | 레이펄스 워터리스 바디워시 200ml (본품) | 확인됨(`product_candidates`/`draft_json`) |
| 브랜드 | 레이펄스 | 확인됨 |
| 카테고리(잠정) | 바디워시/샴푸 | 확인됨(잠정 — 쿠팡 등록 카테고리 코드 매핑은 별도) |
| 옵션 4종(과거 관측) | `3945578` 샴푸 200ml 본품 7,150원 / `3945579` 샴푸 500ml 리필 11,000원 / `3945580` 바디워시 200ml 본품 5,050원 / `3945581` 바디워시 500ml 리필 7,850원 | **과거 API 관측(시점 미상, 2026-09-21 문서 복원) — 최신 아님, 재확인 전 사용 금지** |
| 옵션별 재고(`qty`) | null(과거 관측) | **미확인** — 등록 차단 사유 |
| SKU | Wizard `sku_list=[]` | **미입력** — 등록 차단 사유 |
| 구성수량 | 미확인 | **미확인** — 등록 차단 사유 |
| 공급가 | 과거 옵션 `price`가 실제 공급가와 같은지 미확인 | **미확인** — 등록 차단 사유 |
| 판매가 | 미정(쿠팡 수수료율 미조사, 목표 마진 미산정) | **미확인** — 등록 차단 사유 |
| 배송비(참고) | 기본 3,000원(과거 API), 공개화면 제주+6,000/도서산간+8,000(지역가산 의미 미확정) | 참고용, 확정 아님 |
| 반품조건 | 왕복 6,000원(제주 10,000/도서산간 16,000), 박스훼손 불가 조건은 이 상품엔 명시 없음(다른 두 후보에는 있음) | 확인됨(공개화면 기준, 2026-09-20 조사) |
| 자료 이용권리 | Wizard의 `liveImageRightsConfirmed`류 라이브 필드 미설정(13차 코드 기준 라이브 전송 전 필수 입력) | **미확인/미설정** — 등록 차단 사유 |
| 판매 제한(판매자 자격) | 화장품통신판매업 신고 등 요건 조사 기록 없음 | **미확인** — 등록 차단 사유 |
| 상품고시 | 책임판매업자 유레카코스/제조국 KR/사용기한 3년/KC 인증대상 아님(공개화면) — 사용방법·주요성분·주의사항은 "상세페이지참조"로 **상세 이미지 확인 전 정보 부재** | 부분 확인, 나머지 미확인 |
| 판매신청 상태 | 내부 행 0건, `NEEDS_REVIEW` 미기록 | **차단**(§④) |
| 발주 승인 | `PurchaseOrderApproval#1`, `expires_at=2026-09-14 10:09`(만료, DB엔 `ACTIVE`로 남아있는 지연평가 구조) | **만료 — 재사용 금지**, 재등록·재승인 시점에 완전히 새로 받아야 함 |
| 연결 인증 최신성 | `verified_at=2026-09-08`, 16일 경과(24시간 재검증 창 초과) | **재검증 필요**(외부 확인 C의 부수 효과로만 갱신 가능) |

**미확인 값은 추측하지 않는다** — 위 표의 "미확인" 항목은 전부 등록을
막는 사유로만 남기며, 임의 기본값이나 과거 값을 현재 값처럼 대체하지
않았다.

## ④ 판매신청 확인 근거 확보 여부

**확보하지 못했다** — 계정·상품이 일치하는 공식 확인 근거(온채널
판매자센터 화면 직접 확인, 또는 공급처 공식 문의 회신)가 아직 없다.
`NEEDS_REVIEW` 자체도 원본에 기록되지 않았으므로(승인안 B 대기), 지금은
"검토 대기 상태를 해제할 근거"를 논할 단계 이전이다 — 근거가 없으므로
차단 상태를 그대로 유지한다.

- **확인 방법 정리(문의 발송은 이번 범위 밖, 별도 승인 대상)**: (1)
  온채널 판매자센터 화면에 사람이 직접 로그인해 신청 이력 확인, (2)
  온채널 공급처(또는 담당 부서)에 CH1147184의 판매신청 접수/승인 여부를
  공식 문의. 두 방법 모두 이번 라운드에서 실행하지 않았다(문의 초안은
  기존 D3 문서에 이미 있음, 재작성하지 않음).
- **최초 신청 체크박스로 우회하지 않음**: 17차에서 만든 "최초 신청 확인"
  체크박스는 CH1147184처럼 과거 접수 증거가 있는 상품에는 사용하지
  않는다 — 이 판단은 사람이 해야 하며, 코드가 대신 결정하지 않는다(그
  판단을 코드로 강제할 방법이 없다는 것 자체가 승인안 B가 필요한
  이유다).

## ⑤ 다음 실제 상품등록에 필요한 최소 승인

**A/B/C 승인 요청(기존 문서와 완전히 동일한 대상, 재작성 아님)**:

- **승인안 A**: 원본 `homez.db`에 Migration 3건 적용(백업 후, 파일명·
  순서·해시 §① 재확인 완료). 상세: `docs/HOMEZ_V7_FIRST_APPLICATION_
  UI_AND_REVIEW_RELEASE_20260924.md` §⑤.
- **승인안 B**: `company_id=1`/`connection_id=4`/`product_code=CH1147184`에
  대해 `record_unconfirmed_prior_evidence()`로 `NEEDS_REVIEW` 기록(검토
  해제는 별도, 이번에 함께 하지 않음).
- **승인안 C**: 연결 id=4로 CH1147184 상품 조회 1회(`lookup_product()`,
  자동 재시도 없음, 부수 효과는 연결 `verified_at`/`status` 갱신뿐 —
  판매신청 승인 증거로 쓰지 않음).

**A/B/C 이후, 실제 상품등록 전에 추가로 필요한 것(승인과 별개로 남은
작업, 참고용)**:
1. §④의 확인 방법 중 하나로 실제 판매신청 상태 확인(별도 승인 필요).
2. 확인 결과에 따라 검토 해제(17차 `resolve_needs_review_as_confirmed_
   submitted()`, 근거 있을 때만) 또는 차단 유지.
3. Wizard#1 재개 — 옵션 4종 가격·재고 재조회(§③ "과거 관측" 항목
   전체), SKU·구성수량 확정, 자료 이용권리 확인, 판매 제한(판매자 자격)
   조사, 새 `PurchaseOrderApproval` 발급(기존 것은 만료로 재사용 불가).
4. 이 모든 것이 채워진 뒤에만 13차 중복등록 방어가 적용된 상태로 실제
   쿠팡 등록을 시도한다(이번 A/B/C 범위에 포함하지 않음, 17차 §⑤의
   6단계 순서 그대로 유지).

## 검증·Git

이번 라운드는 코드를 변경하지 않았다 — 전체 회귀·집중 회귀를 재실행하지
않았다(17차의 189건 결과를 그대로 재사용). 임시 서버·감시 작업을 띄우지
않았다(`preview_start` 미사용, `python.exe` 0개로 시작·종료 재확인).

이번 라운드 소유 파일만 커밋한다: 이 문서, `docs/HOMEZ_PROJECT_STATE.md`
갱신. DB·백업·자격증명·개인정보는 포함하지 않는다. 다른 작업자의
워크트리는 건드리지 않았다.
