# HOMEZ V7 — 온채널 회신 대조 및 상품등록 최종 준비 (2026-09-24, 24차)

23차가 보낸 온채널 문의([스레드 `1a0d2d65922b728e`](HOMEZ_V7_CH1147184_LIVE_REQUERY_AND_INQUIRY_20260924.md))
에 아직 회신이 없다. 같은 조회·문의·회귀를 반복하지 않고, 기존 자료
만으로 대조표와 등록 초안을 마무리했다. 실제 자격증명 사용·추가
인증 호출·원본 변경은 전혀 실행하지 않았다.

## 기준선(①)

`git fetch` 후 재확인: 로컬 HEAD = 원격 `origin/main` HEAD = `0a9e8d2`
(23차 커밋, 가정하지 않고 직접 확인). 다른 작업자 워크트리 3개
보존, 시작 시점 `python.exe` 0개.

## 회신 확인

23차가 보낸 스레드(`1a0d2d65922b728e`)를 `get_thread`로 재조회했다
(23차에 이미 명시적으로 승인·실행된 발송의 결과를 확인하는 것이라
별도 메일함 전체 조회가 아님 — 그 스레드 하나만 조회). **결과: 내가
보낸 메시지 1건만 있고, 온채널의 회신은 아직 도착하지 않았다.**

**"미수신 확정"이 아니라 "2026-09-24 기준 확인 불가"로 남긴다** — 이
업체가 메일에 회신하는 데 통상 며칠이 걸릴 수 있고, 이번 확인은
발송 후 약 9시간 시점의 스냅샷일 뿐이다.

## ② 회신 대조표

| 항목 | 답변 원문 근거 | 적용 대상 | 확인 결과 | 남은 질문 |
|---|---|---|---|---|
| 연결 #4 계정의 CH1147184 판매신청 상태 | **없음(회신 없음)** | — | 미확인 — `NEEDS_REVIEW` 그대로 유지(23차 로컬 재확인, 변경 없음) | 문항 1 그대로 유지, 재발송하지 않음 |
| `option_price`/`disc_price`의 의미·적용 조건 | **없음(회신 없음)**. 23차 API 재조회로 "4옵션 전부 `disc_price` > `option_price`"라는 **관측**은 있으나 이는 온채널의 답변이 아니라 HOMEZ가 직접 관찰한 패턴이다 | — | 미확인 — 의미·적용조건 모두 근거 없음 | 문항 2 그대로 유지 |
| 쿠팡 판매 허용 여부·제한 | **없음(회신 없음)** | — | 미확인 | 문항 3 그대로 유지 |
| 이미지·상세설명 이용 범위·수정 허용 여부 | **없음(회신 없음)**. 23차가 이미지 URL 자체는 확보했으나 그것은 "위치"일 뿐 "이용권리"가 아니다 | — | 미확인 | 문항 3과 동일 문항으로 유지(중복 문항 아님) |
| 옵션 3945580 용량·구성수량 | **없음(회신 없음)**. 용량(200ml)은 23차 API 응답(옵션 라벨·`gosi_info.prd_weight`)으로 **공급처 API 기재 확인** 수준까지는 확보됨. 구성수량(낱개/세트 여부)은 전용 필드가 없어 여전히 추정 | 옵션 `3945580` | **부분 확인** — 용량은 닫음(§③ 표기 기준), 구성수량은 열어 둠 | 구성수량만 남겨 문항 4 유지 |

**5개 항목 중 회신으로 새로 해소된 것은 0건이다.** 일반 안내문·과거
API 응답을 "이 계정에 대한 확정 답변"으로 확대하지 않았다. 후속
질문은 기존 4문항을 그대로 유지하며(구성수량 항목만 "용량"을 빼고
"구성수량"으로 범위를 좁혔을 뿐, 내용 자체는 이미 23차에 보낸
그대로), **자동으로 재발송하지 않았다.**

## ③ 상품정보 확인 수준 — 표현 재조정

23차 문서의 일부 표현이 실제 확인 수준보다 강하게 들렸던 부분을
아래 기준으로 다시 정리한다(23차 문서 자체는 수정하지 않고, 이
문서에서 적용 기준을 명시한다):

| 항목 | 정확한 확인 수준 |
|---|---|
| 제조사(유레카코스)·원산지(대한민국)·사용기한(제조일로부터 3년)·반품조건(왕복 6,000원 등) | **"공급처 API(`gosi_info`/`return_comment`) 기재 확인"** — 온채널이 자사 시스템에 그렇게 기재해 뒀다는 뜻이다. 실물 제품 라벨·인증서·제3자 검증과는 별개이며, 그 수준까지 확인됐다는 뜻이 아니다 |
| `disc_price`가 `option_price`보다 높다는 관측 | **패턴 관측일 뿐**이다 — "그러므로 최저판매가 기준이다"로 확정하지 않는다. 의미는 여전히 미확인(§②)이며, 온채널 회신으로만 확정 가능하다 |
| `recom_cus_price=0`(4옵션 전부) | **가격 데이터로 쓰지 않는다** — 무료도, 정상 권장가 0원도 아니다. "그 필드가 채워지지 않았다"는 뜻일 가능성이 높다는 정도의 참고일 뿐, 확정된 사실이 아니다 |
| 책임판매업자·제조사 | `gosi_info.make_import` 원문은 "화장품 제조업자 및 화장품 책임판매업자 유레카코스"로 **두 역할을 한 필드·한 값에 함께 기재**했다 — 이 원문 자체가 두 역할을 분리하지 않았으므로, HOMEZ도 "제조사=유레카코스"와 "책임판매업자=유레카코스"를 **각각 독립적으로 검증한 것처럼** 임의로 나누지 않는다. 원문 그대로 "이 상품은 제조사·책임판매업자가 함께 유레카코스로 기재됨"까지만 사실로 취급한다 |

## ④ 등록 초안 마무리(옵션 3945580, 원본 미저장)

23차 초안을 위 확인 수준 기준으로 다시 정리한다. Wizard#1(`current_step
=CHANNELS`, 미변경)과 옵션 3945580을 기준으로 한다.

| 필드 | 값 | 확정값/가정값/미확인 | 근거·확인 수준 |
|---|---|---|---|
| 상품명 | "레이펄스 워터리스 바디워시 200ml (본품)" | 확정값(원본 그대로, 변경 없음) | `product_candidates#3`(기존) |
| 카테고리(잠정) | 바디워시/샴푸 | 미확인(쿠팡 카테고리 코드 미매핑) | 기존 `category_hint` |
| 대상 옵션 | `3945580` | 확정값 | 23차 API 재조회, HOMEZ 후보명과 문자 일치(22차 확인) |
| 판매가(원가 기준값) | `option_price`=5,050원 | **공급처 API 기재 확인** | 3회 관측 일치(19·21·23차) |
| 배송비 | 기본 3,000원/제주 6,000원/도서산간 8,000원 | 공급처 API 기재 확인 | 23차 재조회 |
| 반품조건 | 왕복 6,000원(제주 10,000/도서산간 16,000) | 공급처 API 기재 확인(격상, §③) | 23차 `return_comment` 원문 |
| 용량 | 200ml | 공급처 API 기재 확인 | 23차 옵션 라벨·`gosi_info.prd_weight` |
| 구성수량 | 1개(단품, 추정) | **미확인** | 전용 필드 없음, §② 문항 4 회신 대기 |
| 제조사(제안, 미반영) | 유레카코스 | 공급처 API 기재 확인(제안값, `resolve_run()` 미실행) | §③ 원문 |
| 원산지(제안, 미반영) | 대한민국 | 공급처 API 기재 확인(제안값, 미반영) | §③ 원문 |
| 사용기한 | 제조일로부터 3년 | 공급처 API 기재 확인 | §③ 원문 |
| KC 인증 | 대상 아님 | 공급처 API 기재 확인 | 23차 `gosi_info.kc_sec` |
| 기능성 화장품 | 해당사항 아님 | 공급처 API 기재 확인 | 23차 `gosi_info.func_cos_sec` |
| 이미지 | URL 확보(`image.onch3.co.kr/...`) | **위치만 확인, 이용권리 미확인** | §② 회신 대기 |
| 상세설명·사용법·주의사항 | **미확인**("상세페이지참조"만 반환) | 미확인 | `contents` 필드 미매핑(범위 밖) |
| 쿠팡 판매 가능 여부 | **미확인** | 미확인 | §② 회신 대기 |
| 속성비교 | `BLOCKED`(run#2, NAME 외 전부 소스 0개) | 미확인(원본 미반영) | 22·23차 |
| 판매신청 | `NEEDS_REVIEW`(유지) | 미확인 | §② 재확인, 변경 없음 |

### 원가·판매가 계산(확정값/가정값 구분, 변경 없음)

```
원가(공급처 API 기재 확인) = 5,050원 + 3,000원(배송비) = 8,050원
목표마진율(m, 가정) = 30%  ← 정책 미확정(purchase_task_policy_settings 여전히 0/0)
채널수수료율(f, 가정) = 10.8% ← 과거 관측값일 뿐, 확정 수수료율 아님
예시 판매가 = 8,050 / (1 - 0.30 - 0.108) ≈ 13,598원(확정 아님, 참고용)
```

`disc_price`(6,500원)는 의미가 여전히 미확인이므로 원가에서 차감하지
않는다(§③). 세금·반품비·광고비 미포함, 반품 발생 시 왕복 6,000원
별도.

### 내부 SKU(기존 규칙 재사용, 변경 없음)

`PC3-CH1147184-3945580` — 저장하지 않음. 쿠팡 발급 식별자
(`externalVendorSku`/`vendorItemId`)는 만들지 않았다.

### 등록 가능 여부 판정

**"등록 가능"으로 판정하지 않는다.** 필수로 남은 미확인 항목이
있다: 구성수량, 이미지·상세설명 이용권리, 상세설명 본문, 쿠팡 판매
가능 여부, 판매신청 상태, 속성비교 원본 미해소. 이 중 하나라도
남아 있으면 실제 등록의 필수 정보가 갖춰지지 않은 것으로 본다.

## ⑤ 실제 실행 승인안 — 이번 라운드에서 새로 승인 가능해진 항목 없음

회신이 없으므로 "답변으로 조건이 충족된 항목"은 0건이다. 4개
승인안의 현재 상태만 정확히 남긴다(전부 미실행, 각각 독립):

| 승인안 | 현재 상태 | 남은 전제조건 |
|---|---|---|
| **A. 속성비교 값 선택**(대상: run#2, MANUFACTURER/ORIGIN_COUNTRY) | 후보값은 23차에 이미 확보(§③ "공급처 API 기재 확인" 수준) — **다만 이는 온채널 회신이 아니라 API 재조회 근거**이므로, 이번 라운드 기준("답변으로 조건 충족")으로는 신규 승인 대상에 올리지 않는다. 사용자가 원하면 이 근거만으로 별도 판단할 수 있다는 사실만 남긴다 | 사용자의 명시적 결정(회신과 무관하게 지금 결정 가능) |
| **B. 판매신청 검토 해제** | 불가 | §② 문항 1 회신 필요(공식 확인 근거 없음) |
| **C. Wizard#1 저장** | 불가(정확한 필드·값은 §④ 표로 준비 완료) | 미승인 항목(이번 라운드에서도 승인 대상 아님), 필수 미확인 항목 다수 잔존 |
| **D. 쿠팡 상품등록** | 불가 | B·C 선행 + §② 문항 1·3 회신 필요 + 쿠팡 연동 현재 유효성 재확인(22차) 필요 |

검토 해제만으로 발주가 실행되거나 초안 저장만으로 외부 등록이
실행되는 구조가 아님을 코드로 이미 확인했다(21~22차) — 이번
라운드는 그 경계를 다시 무너뜨리지 않았다(전부 미실행).

## ⑥ 과거 계정 ID 노출 점검

`docs/` 전체에서 평문 계정 아이디(온채널 계정) 출현을 검색했다 —
**12개 문서, 23곳**에서 발견(18~24차에 걸친 과거 작업 문서 포함).
전부 리터럴 문자열 치환으로 마스킹했다(`sed`, 기계적 전수 치환 —
계정 아이디 외 다른 텍스트는 건드리지 않음, 파일당 diff가 정확히
발견 건수와 일치함을 확인). Git 이력 재작성·강제 push는 하지
않았다 — 과거 커밋에는 여전히 원문이 남아 있으나, 이는 이번 승인
범위(현재 문서 마스킹) 밖이다.

**위험 등급 판단**: 계정 아이디는 비밀값이 아니다(로그인 비밀번호나
API 키가 아니라 온채널 플랫폼상의 사용자 식별 라벨) — 노출돼도
그 자체로 계정을 탈취할 수 없다. 그럼에도 불필요한 노출을 줄이는
차원에서 마스킹했다. **별도로 API 키·비밀번호·시크릿 패턴을
`docs/` 전체에서 검색했으나 실제 노출은 발견되지 않았다** — 매치된
5개 파일은 전부 (a) UI 기능 설명(비밀번호 재설정 안내), (b) 코드
수정 이력 설명(로그인 폼 필드 클리어), (c) 마스킹 메커니즘 자체에
대한 문서화, (d) 테스트/제안 코드의 명백한 더미 값(`"access_key":
"access", "secret_key": "secret"`)이었다 — 실제 시크릿 노출
0건이므로 별도 대응으로 분리할 사안이 없다.

## ⑦ 실행 관리·종료

- 이번 라운드는 코드 변경이 없어 테스트를 재실행하지 않았다.
- 실제 외부 호출: 기존 스레드 조회 1회(신규 발송·신규 조회 없음).
- 로컬 DB 변경: **없음**(이번 라운드는 문서 마스킹·정리만 수행,
  DB에 접근하지 않았다).
- 회신이 없다고 대기 루프나 예약 알림을 만들지 않았다 — 정상
  종료한다. 다음 라운드에서 다시 그 스레드를 확인하면 된다.
- 자신이 시작한 임시 작업 없음(`python.exe` 0개 유지).

## 최종 보고

① **회신 확인 여부**: 미도착(2026-09-24 기준 확인 불가로 기록,
미수신 단정 아님).

② **해결된 항목 / 답변 부족 항목**: 회신으로 새로 해결된 항목
0건. 5개 대조 항목 중 4개는 완전 미확인, 1개(옵션 3945580 용량·
구성수량)는 용량만 "공급처 API 기재 확인" 수준으로 부분 확인.

③ **등록 초안 완성도**: 옵션 3945580 기준 초안을 확정값/가정값/
미확인값으로 구분해 문서로 완성(원본 미저장). 정확한 차단 사유:
구성수량 미확인, 이미지·상세설명 이용권리 미확인, 상세설명 본문
미확보, 쿠팡 판매 가능 여부 미확인, 판매신청 `NEEDS_REVIEW`,
속성비교 `BLOCKED`(원본 미해소) — "등록 가능"으로 판정하지 않았다.

④ **지금 승인 가능한 실행 목록**: 없음. A(속성비교 값 선택)는
근거는 있으나 이번 라운드 기준(회신 확인)으로는 올리지 않았고,
사용자가 별도로 판단할 수 있다는 사실만 기록했다. B·C·D는 전제조건
미충족으로 불가.

⑤ **다음 단계**: 온채널 회신 대기(자동 재조회·재문의 없음). 회신이
오면 §② 대조표를 갱신하고, 해결된 항목만 닫아 §⑤ 승인안을 다시
정리한다. 사용자가 A(속성비교 제안값 반영)를 회신과 무관하게 먼저
결정하고 싶다면 그 결정만 별도로 받을 수 있다.

## Git

이번 라운드는 이 문서 1개 신규 생성 + 계정 아이디 마스킹(12개
기존 문서, `docs/HOMEZ_PROJECT_STATE.md` 포함) + `docs/HOMEZ_PROJECT_STATE.md`
상단 요약 갱신만 커밋한다. 코드·DB·개인정보·자격증명·원문 메일은
포함하지 않는다.
